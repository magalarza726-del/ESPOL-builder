// Compatibility facade retained for old bookmarks/caches.
// Gameplay state, UI synchronization and input now live in runtime.js.
export { GAME_MODES, MODE_IDS, getModeConfig, isGameMode } from './modes.js';
