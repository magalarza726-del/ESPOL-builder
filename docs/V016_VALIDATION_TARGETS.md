# v0.16 validation targets

Release blockers:
- public import map must use game3d_v016.js;
- project metadata/audit must be v0.16;
- exactly two public modes: Día and Noche;
- FIMCP spawn must use the mapped parking control point;
- FIMCP reconstruction must select only controlled/core GIS footprints;
- scale guards between FIMCP control points must pass;
- npm run verify must pass before Pages deployment.
